"use client";
import React from 'react';
// This is a placeholder file to prevent import errors
// It will be properly removed later after the build
// The actual functionality has been moved to aurora-background.jsx

const SplashBackground = ({ children }) => {
  console.warn('SplashBackground is deprecated, please use AuroraBackground instead');
  return <div>{children}</div>;
};

export default SplashBackground; 